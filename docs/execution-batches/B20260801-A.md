# 总控批次 B20260801-A

更新时间：2026-08-05（UTC+8）
状态：`COMPLETED / R01_R02_R04_R05_R06_MERGED / R03_DEFERRED_BY_USER`
目标仓库：`xxzdecode/xxzcard-en`

## 1. 执行原则

```text
GPT 优先分析、实现、提交和审核
→ 只有 GPT 无法可靠完成的环境验证才调用 Codex
→ Codex 集中做 WebKit、缓存、构建和跨模块验收
→ 测试异常返回 GPT 修复
```

每个需求保持独立 R 编号、分支和 Draft PR，不把新问题塞入既有任务。

## 2. 当前状态

| R | 执行卡 | 需求 | 状态 |
|---|---:|---|---|
| R01 | 016 | iPad 点击词汇探险无法打开 | `MERGED / PR #18` |
| R02 | 017 | 纯听力写单词显示正式音标 | `MERGED / PR #21` |
| R03 | 018 | 统一 CLEAR 完成效果 | `DEFERRED_BY_USER / EXCLUDED_FROM_BATCH` |
| R04 | 019 | 单词挑战金币可靠结算 | `MERGED / PR #19` |
| R05 | 020 | 语法挑战确认前允许改选 | `MERGED / PR #20` |
| R06 | 021 | Cloudflare 静态资源部署配置 | `MERGED / PREVIEW_VERIFIED / PR #22` |

## 3. 批次收口状态

R03 已由用户明确暂停。当前批次不组装 R03 基线、不创建 R03 分支或 PR，也不把 CLEAR 纳入本轮集中 QA 或合并范围。以后只有用户重新明确启动时，才重新读取执行卡并根据当时最新视觉范围实施。

本批次已经结束：PR #18、#19、#20、#21、#22 均已合并到 `main`，不再存在业务分支同步步骤。R03 未进入本批次，只有用户重新明确启动时才按当时主线建立新任务。

历史执行顺序：

```text
R06 已合并到 main
→ 将最新 main 同步到 R01 + R04 + R05
→ 将更新后的 R04 同步到 R02
→ 确认四个业务 PR 的 Cloudflare 部署成功
→ 组装候选基线
→ 单次集中 Codex QA
→ GPT 修复测试问题
→ 最终合并审核
```

## 4. 已审核提交

- R01 / PR #18：`3ccdfdc017afe974978f8dad924f7ed688667c09`
- R04 / PR #19：`89161577e237a5b969bdad100381c0e806b93235`
- R02 / PR #21：`08c7aa2647139c616e430b04245cd4b035c39832`
- R05 / PR #20：`c4090e82555ffcb129bad7403f72eb74a945363e`
- R06 / PR #22 审核 head：`510e6afd68314bf4d52c9ce8db1a6dacfe9c1a81`
- R06 合并提交：`f0fb4350b16c7888dd5481102cfc7c4cf8b87dc0`

合并前 R02 堆叠在 R04 分支；当时的同步顺序是先更新 R04，再把更新后的 R04 同步到 R02，最后将 R02 的 base 改回 `main` 并检查净差异。

## 5. 历史审核记录（合并前）

### R01｜PR #18

- 代码审核通过。
- 已修复同步 `appendChild` 抛错和超时后的重试缓存清理。
- GitHub Actions 通过。
- 待验收：真实 iPad、旧 Service Worker、主屏幕 Web App、慢网与离线重开。

### R04｜PR #19

- 代码审核通过。
- 奖励结算已改为基于正式完成状态，支持 pending、失败重试、max 补差、幂等和账户隔离。
- Supabase 只读复核：姐姐云端只有 `bestScore=100`，正式会话为 `abandoned/correctCount=0`，不满足安全补账条件，未自动补账正确。
- 原设备诊断前不得清除站点数据，也不得用姐姐账号开启新的单词挑战。

### R02｜PR #21

- 代码审核通过。
- 正确基于 R04 head 堆叠实施。
- 探险和挑战的 `audioSpelling` 统一读取 `card.phonetic`；空值隐藏，不改变判分、奖励和完成状态。
- 待验收：真实软键盘、iPad 1180×820、长音标压缩、完整探险与挑战流程。

### R05｜PR #20

- 返工复审通过。
- 目录入口和正式 practice 直链统一使用共享运行时。
- 单选、多选、排序、分类确认前可修改；点击“下一题”才锁定、判分并写入一次历史。
- 原始直链浏览器回归和三项 GitHub Actions 通过。

### R06｜PR #22

- 根因：Cloudflare 默认 `npx wrangler deploy` 找不到 Worker `main` 或静态资源目录。
- 新增 `wrangler.jsonc`，以 Workers Static Assets 部署仓库根目录。
- 新增 `.assetsignore`，排除测试、文档、项目规则、依赖和部署配置文件。
- 修复未锚定 `docs/` 误排除正式 token CSS 导致的首页圆角回归，改为仅排除仓库根 `/docs/`。
- Cloudflare 对审核 head `510e6afd68314bf4d52c9ce8db1a6dacfe9c1a81` 部署成功，原缺少入口错误解除。
- 人工复验通过：Commit Preview 与 Branch Preview 圆角正常、视觉一致；`package.json` 与根 `docs/` 内部文件无法公开访问。
- PR #22 已合并到 `main`，合并提交为 `f0fb4350b16c7888dd5481102cfc7c4cf8b87dc0`。

## 6. 发布状态（已收口）

PR #18、#19、#20、#21 原先的 Cloudflare 失败来自同一仓库级配置缺陷：

```text
Missing entry-point to Worker script or to assets directory
```

R06 完成 Preview 验证并合并到 `main` 后，R01、R04、R05 及堆叠的 R02 已完成同步和最终合并。当前文件只作历史追溯，不再驱动分支、PR 或集中 QA。

## 7. 本轮集中验收范围

- R01：WebKit、冷缓存、旧缓存升级、失败重试、重复点击、离线重开；
- R02：有/无音标、切题残留、输入焦点、软键盘、长音标与完整流程；
- R04：奖励保存失败、自动补发、最高值补差、账户隔离、老师覆盖和幂等；
- R05：目录入口与直链中的各题型改选、确认、双击提交和历史记录；
- R06：Cloudflare 静态资源部署、首页与正式子页面加载、内部文件不公开；
- 全局：首页、词汇探险、单词挑战、语法挑战和规定视口无回归。

真实 iPad Safari 最终人工检查仍需在实际设备完成。R03 不在本轮验收范围。

## 8. 最小回执

```text
Rxx｜PR #编号｜异常说明
```
